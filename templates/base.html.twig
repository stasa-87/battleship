<!DOCTYPE html>
<html>
    <head>
        {% block head %}

            <link rel="stylesheet" href="{{ absolute_url('/assets/bootstrap-reboot.min.css') }}" />
            <link rel="stylesheet" href="{{ absolute_url('/assets/bootstrap.min.css') }}" />
            <link rel="stylesheet" href="{{ absolute_url('/assets/bootstrap-grid.min.css') }}" />
            <title>{% block title %}{% endblock %} - Battleships</title>
        {% endblock %}
    </head>
    <body>
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </body>
</html>